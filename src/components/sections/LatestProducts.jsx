import React from 'react';

const LatestProducts = ({ title }) => {
    return (
        <section>
            <h1>
                { title }
            </h1>
        </section>
    );
}

export default LatestProducts;